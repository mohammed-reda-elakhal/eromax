const asyncHandler = require("express-async-handler");
const {Admin, adminValidation}=require("../Models/Admin");

/** -------------------------------------------
 *@desc get list Admin   
 * @router /api/admin
 * @method GET
 * @access private Only admin
 -------------------------------------------
*/

// Controllers if we need them 

