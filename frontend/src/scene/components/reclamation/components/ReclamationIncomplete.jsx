import React from 'react';

function ReclamationIncomplete({ theme }) {
    // This component is now empty as we've integrated its functionality
    // directly into the main Reclamation component
    return null;
}

export default ReclamationIncomplete;
